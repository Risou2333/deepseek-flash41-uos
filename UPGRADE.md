# 从 1.0 升级到 1.1

1. 在旧服务终端按 Ctrl+C。
2. 建议先复制备份个人目录的 `.local/share/deepseek-client` 文件夹（含配置和历史）。
3. 将新版 ZIP 解压至新文件夹。在其中 `deepseek-client` 文件夹打开终端，运行 `bash start.sh`。
4. 确认侧栏显示 1.1.0，登录后旧记录仍可打开。首次迁移前程序也会建立历史数据库副本。
5. 若使用应用菜单启动，在新版程序目录执行 `python3 install-desktop.py`，更新旧菜单入口指向。
6. 打开“连接与用量诊断”，先查询模型/余额，再发送一条计费测试，查看真实返回的用量信息。

没有 .deb 安装步骤。已有用户无需重填 Key；需要修改时点侧栏“Key 与密码”。全新用户在首次网页上设置 Key 与密码。

若提示端口被占用，先核实旧服务是否关闭，不要盲目杀进程。若之前配置了 DEEPSEEK_DATA_DIR，启动新版时继续使用相同变量，否则会读取默认目录。

本版默认 high 思考、输出跟随 API 默认、无额外风格提示。此前固定 8192 输出上限与默认关闭思考已调整。旧回复不能补出不存在的调用日志，新回复才记录返回模型与用量。
