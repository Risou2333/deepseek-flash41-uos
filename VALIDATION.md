# 1.1.0 验证记录

日期：2026-09-10。执行环境：Linux / Python 3.12.14。目标环境：用户提供的 UOS 20 / MIPS64 小端 / Python 3.7.3。

## 17 项测试通过

命令：`python3 -m unittest discover -s tests -v`。

- 登录、Host/Origin 写入保护。
- 流式回答、成功问答成对保存与删除。
- 取消不保存、断流不保存与生成锁释放。
- 完整问答对上下文裁剪。
- 文本/Office 提取与损坏、超限处理。
- 默认 Flash/high 请求、API 默认输出上限、关闭与 max 思考、JSON 参数。
- 非法设置拒绝、超出本轮预算拒绝。
- 图片真实文件头检查与历史图片构造/省略。
- SSE 注释、空行、CRLF、多行事件解析。
- API 返回 model、响应 ID、usage、reasoning 持久保存。
- length 截断 JSON 正文保持原样、标记无效 JSON。
- 模型列表与余额查询路径、API 错误提示。
- 网页初次设置不可重复覆盖、更换 Key、旧登录失效、配置权限与 Key 不回传。
- 旧版有消息的数据库迁移、备份、重复初始化不重复迁移。
- 对话设置保存和重命名。

测试均使用临时数据库/模拟上游，不读写用户真实数据，不调用付费模型。图片测试验证消息结构与文件头检查，不代表模型已成功识别实际图片。

## 静态检查

- 所有交付 Python 文件通过 Python 3.7 语法解析；主程序编译通过。
- JavaScript `node --check` 通过。
- `start.sh`、`diagnose.sh` Shell 语法检查通过。
- HTML id 唯一，JavaScript 引用的固定 DOM id 均存在。
- Markdown 使用 DOM 文本节点构建，不使用 innerHTML。
- 打包排除旧 .deb、旧构建脚本、数据库、Key、缓存、临时测试服务与修改脚本；ZIP 完整性检查通过。

## 未验证的内容

- 没有可用的真实浏览器引擎，未完成浏览器截图、交互和布局自动测试。静态 DOM 检查不能代替浏览器实测。
- 未在用户 MIPS64 / Python 3.7.3 环境运行或测量性能。
- 未持有用户 API Key，未验证其余额、真实生成用量和“无消耗”的原因；需要新版诊断与计费测试的实际结果。
- PDF 依赖用户系统 pdftotext；复杂文档、扫描图片识别和账单精确金额未经实测。

## 最短验收顺序

1. 停止旧服务，在新版目录执行 `bash start.sh`。
2. 新用户网页配置，老用户直接登录；确认侧栏 1.1.0。
3. 查询模型与余额，再手动发送计费测试。
4. 检查该条回复调用详情中的 requested_model / returned_model / response_id / usage。
5. 新对话 high 思考，上传一张小图，追问图片，再刷新查看历史。
6. 修改对话设置，导出 MD/JSON；按需更换 Key 后重新登录。

故障反馈不要附上 config.json、Key 或密码；导出 JSON 包含原图、材料和思考，分享前核对内容。
